# TODO Review Test

Small React + TypeScript project designed to test code review tools.

This repo intentionally contains a few issues that a reviewer should detect:
- missing React list keys
- weak variable naming
- non-functional React state updates
- duplicated calculations
- basic accessibility gaps

Run locally:

npm install
npm run dev
